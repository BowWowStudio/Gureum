export interface Event {
  index: number;
  submittedDate: Date;
  deliverDate: Date;
  description: string;
  title: string;
}
export interface Post {
  title: string;
  date: Date;
  body: string;
}
